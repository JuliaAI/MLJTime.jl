using MLJTime

const univariate_data = univariate_datasets

TSdataset(univariate_data)


function Benchmake_all(algorithms)
    Dict_measures = Dict{String, Integer}()
    for algo in algorithms
        for     
end
